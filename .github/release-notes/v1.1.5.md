# Komari-Theme-Aster v1.1.5

本版本重点优化首页小卡片性能，并新增连接数显示配置。

## 主要更新

1. 优化首页小卡片滚动性能，减少离屏卡片的渲染开销。
2. 优化小卡片 CPU / 内存 / 硬盘 / 负载进度条，减少大量 DOM 节点。
3. 移除小卡片流量点的持续动画，降低滚动时的绘制压力。
4. 新增 TCP / UDP 连接数显示开关，默认关闭，可在主题设置中启用。
5. 大卡片和小卡片支持展示实时 TCP / UDP 连接数。
6. 优化 Ping 绑定页面的节点筛选性能，节点较多时操作更流畅。
