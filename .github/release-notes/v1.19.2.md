# Aster v1.19.2

修复删除 Ping 延迟任务后首页仍显示“任务 #ID”的残留数据。

- 主页仅使用服务端仍存在的任务元数据；已删除任务即使残留历史样本也不会再参与延迟、丢包、排序或来源展示。
- 主题管理页会识别失效的任务绑定，首页立即忽略，并在保存设置时清理对应绑定、主任务和展示分组。
- 补充了删除任务的回归测试，覆盖“历史记录仍在、任务元数据已不存在”的场景。

建议同时升级 Komari 面板到 v1.2.13 或更新版本，以在数据源端完成原子清理和迟到结果拦截。
