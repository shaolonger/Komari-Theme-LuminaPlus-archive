# Aster v1.19.1

修复 VPS 列表视图的列对齐问题。

- 桌面端表头与数据行现在共用同一份独立列网格。
- 上传、下载、延迟、丢包、在线、到期、费用均为独立列，不再在指标组内纵向堆叠。
- 中等宽度会把延迟、丢包、在线、到期、费用移入第二条严格对齐的指标带，避免横向滚动。
- 保持移动端紧凑重排、列表排序和三种视图切换能力。

验证：通过 251 个单元测试、ESLint、TypeScript 类型检查和生产构建；1720px、1024px、375px 均确认无横向溢出，桌面完整列的表头与数据列边界差为 0px。
